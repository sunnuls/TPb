# Тестовый Режим Лаунчера (Test Mode)

## Быстрый запуск без настройки ROI

Если у вас не открывается окно настройки ROI или вы хотите протестировать интерфейс лаунчера без реального клиента покера, используйте **Тестовый Режим**.

---

## Инструкция по запуску

### 1. Запустите лаунчер

```bash
START_LAUNCHER.bat
```

### 2. Создайте тестовые аккаунты

**Вкладка "Accounts":**

1. Нажмите `➕ Add Account`
2. Введите:
   - **Nickname**: `TestBot001`
   - **Room**: `pokerstars` (или любой другой)
   - **Notes**: `Test account for demo`
3. Нажмите `Save`
4. Повторите для создания 2-3 аккаунтов

### 3. Пропустите настройку ROI

**Для каждого аккаунта:**

1. **Выберите аккаунт** в таблице (кликните на строку)
2. Нажмите **`⚠️ Skip ROI (Test Mode)`** (оранжевая кнопка)
3. Подтвердите в диалоге:
   ```
   Skip ROI for TestBot001?
   
   This will create DUMMY ROI zones for testing.
   Bot will NOT be able to interact with real poker client.
   
   Use this ONLY for testing the launcher interface.
   ```
4. Нажмите **`Yes`**

**Результат:**
- Статус аккаунта изменится на `READY`
- Окно (Window): `Test Window (Dummy)`
- ROI: `✓` (зелёная галочка)

### 4. Запустите ботов

**Вкладка "Bots Control":**

1. Аккаунты должны автоматически загрузиться как боты
2. Установите количество ботов для запуска: `1` или `2`
3. Нажмите **`🚀 Start Selected`**

**Результат:**
- Статус ботов: `RUNNING`
- Table: `TestTable_001` (фиктивный стол)
- Hands: 0 → начнёт увеличиваться (симуляция)
- Profit: $0.00 → будет изменяться (симуляция)

### 5. Проверьте Dashboard

**Вкладка "Dashboard":**

- **Active bots**: 1 (или сколько запустили)
- **Total hands**: растёт
- **Total profit**: изменяется
- **Win rate**: рассчитывается
- **Vision errors**: 0
- **Alerts**: отображаются если есть

### 6. Проверьте логи

**Вкладка "Logs":**

- Цветные логи в реальном времени
- Фильтрация по уровню: `DEBUG`, `INFO`, `WARNING`, `ERROR`, `CRITICAL`
- `Clear Logs` - очистить
- `Export Logs` - сохранить в файл

### 7. Остановите ботов

**Вкладка "Bots Control":**

1. Выберите бота в таблице
2. Нажмите `⏹ Stop Selected` или
3. Нажмите глобальную кнопку `🚨 EMERGENCY STOP` (для всех ботов)

---

## Ограничения тестового режима

**⚠️ ВАЖНО:**

- Боты **НЕ** взаимодействуют с реальным покерным клиентом
- Все действия **СИМУЛИРУЮТСЯ** (фиктивные столы, карты, действия)
- Vision capture **НЕ РАБОТАЕТ** (dummy zones)
- Кнопки действий (Fold/Call/Raise) **НЕ НАЖИМАЮТСЯ**
- Collusion/Manipulation **НЕ ТЕСТИРУЮТСЯ**

**Этот режим для:**
- Проверки интерфейса лаунчера
- Тестирования создания/запуска/остановки ботов
- Просмотра Dashboard и логов
- Проверки settings/presets

---

## Переход на реальный режим

Для работы с реальным покерным клиентом:

### 1. Захватите окно покера

**Вкладка "Accounts":**

1. **Откройте покерный клиент** (PokerStars, GGPoker и т.д.)
2. Выберите аккаунт в таблице
3. Нажмите `🖥️ Capture Window`
4. Выберите окно покерного клиента из списка
5. Нажмите `Select`

### 2. Настройте ROI

1. Выберите аккаунт с захваченным окном
2. Нажмите `📐 Configure ROI`
3. **Если окно не открывается** - проверьте логи:
   - `launcher/launcher.log`
   - Ошибки захвата экрана
   - Ошибки `ROIOverlay`

### 3. Отладка проблем с ROI

**Если ROI overlay не открывается:**

1. Проверьте, что окно покера:
   - Не свёрнуто
   - Не закрыто
   - Видимо на экране
2. Проверьте логи после нажатия "Configure ROI":
   ```
   Configure ROI requested for: <nickname>
   Window ID: <id>
   Window Title: <title>
   Is captured: True
   Creating ROI overlay for window <id>
   ROI overlay shown
   ```
3. Если ошибка:
   ```
   Failed to show ROI overlay: <error>
   ```
   - Попробуйте **recapture** окно
   - Перезапустите лаунчер
   - Перезапустите покерный клиент

---

## Структура файлов тестового режима

```
config/
├── accounts.json      # Тестовые аккаунты (dummy window info)
├── roi_configs.json   # Dummy ROI zones
└── settings.json      # Глобальные настройки
```

**Удалите эти файлы**, чтобы сбросить лаунчер:

```bash
del config\accounts.json
del config\roi_configs.json
del config\settings.json
```

---

## Следующие шаги

1. **Тестовый режим работает?** ✅
   - Проверьте все вкладки
   - Протестируйте создание/запуск/остановку ботов
   - Измените settings/presets

2. **Переходите на реальный режим** 🎯
   - Захватите реальные окна
   - Настройте реальные ROI зоны
   - Запустите с флагом `--unsafe` (если нужно)

3. **Изучите документацию** 📚
   - `QUICK_START_LAUNCHER.md`
   - `КАК_ЗАПУСТИТЬ_LAUNCHER.txt`
   - `launcher/README.md`

---

## Помощь

**Если что-то не работает:**

1. Проверьте логи: `launcher/launcher.log`
2. Проверьте консоль при запуске `START_LAUNCHER.bat`
3. Убедитесь, что все зависимости установлены: `SETUP_LAUNCHER.bat`
4. Попробуйте удалить `config/*.json` и начать заново

**Известные проблемы:**

- `keyboard` module syntax error → hotkeys disabled (это нормально)
- ROI overlay не открывается → используйте Test Mode
- Боты не запускаются → проверьте статус `READY` у аккаунтов

---

## Этические предупреждения

**🚨 КРИТИЧЕСКИ ВАЖНО:**

Этот лаунчер управляет системой **КООРДИНИРОВАННОЙ КОЛЛЮЗИИ**.

- **НЕЗАКОННО** в реальном покере
- **КРАЙНЕ НЕЭТИЧНО**
- **ТОЛЬКО ДЛЯ ОБРАЗОВАТЕЛЬНЫХ ЦЕЛЕЙ**

**Никогда не используйте без явного согласия ВСЕХ участников.**

Это исследовательский прототип для изучения:
- Теории игр
- Multi-agent систем
- HCI (Human-Computer Interaction)

---

**END OF TEST MODE GUIDE**
