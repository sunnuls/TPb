# 🔧 Исправление: Захват полного окна (не дочернего)

## Проблема

На скриншотах видно что:
- **Захватывается:** Только игровой стол (1454x1044)
- **Должно захватываться:** Полное окно покера с меню слева

### Причина

При выборе окна покера было выбрано **дочернее окно** (child window) вместо главного окна приложения.

Многие приложения состоят из нескольких окон:
```
Главное окно покера
├── Дочернее окно: Меню слева
├── Дочернее окно: Игровой стол (это было захвачено)
└── Дочернее окно: Панель управления
```

## ✅ Решение

Система теперь **автоматически определяет** дочерние окна и предлагает захватить главное окно!

### Что изменилось:

1. **Автоматическое определение** дочерних окон при выборе
2. **Диалог подтверждения** - система спросит хотите ли вы захватить главное окно
3. **Улучшенный захват** - два метода (PrintWindow + BitBlt)
4. **Детальное логирование** - видно что именно захватывается

## 🚀 Как использовать

### Шаг 1: Откройте приложение

```bash
python -m launcher.ui.main_window
```

### Шаг 2: Захватите окно заново

1. **Accounts** → выберите существующий аккаунт
2. Нажмите **"Capture Window"**
3. Выберите окно покера из списка

### Шаг 3: Диалог автоматического определения

Если вы выбрали дочернее окно, увидите диалог:

```
⚠️ Warning: You selected a child window!

Selected: [какое-то окно]
Size: 1454x1044

Root window found: Kolobok - Бот дизната Хондам 190/320 Хонандон #имена - Подключитель клиенфа
Size: 1920x1080

Child windows may not capture the full application content.

Do you want to capture the ROOT WINDOW instead?
(Recommended for full window capture)

[Yes] [No]
```

**Нажмите "Yes"** чтобы захватить полное окно! ✅

### Шаг 4: Проверка

1. Откройте **Debug Viewer**
2. Нажмите **"📸 Capture Now"**
3. Проверьте захваченное изображение:

**Должны увидеть:**
- ✅ Меню слева со списком игр
- ✅ Игровой стол в центре
- ✅ Все элементы интерфейса

## Технические улучшения

### 1. Определение дочерних окон

```python
# Новая функция в WindowCapturer
def get_root_window(self, hwnd: int) -> int:
    """Получить главное окно для дочернего окна."""
    root = win32gui.GetAncestor(hwnd, win32con.GA_ROOT)
    return root
```

### 2. Автоматическая проверка при выборе

```python
# В WindowSelectDialog._on_select_clicked()
window_info = capturer.get_window_info(window['hwnd'])

if window_info.get('is_child'):
    # Показать диалог с предложением захватить root window
    ...
```

### 3. Улучшенный захват через PrintWindow

```python
# Два метода захвата:
# 1. PrintWindow (предпочтительный) - работает для layered windows
# 2. BitBlt (fallback) - универсальный метод
```

### 4. Детальное логирование

Теперь в логах видно:
```
[INFO] Capturing window: Kolobok - ... (HWND: 123456)
[INFO] Window size (with borders): 1920x1080
[WARNING] This is a child window! Parent HWND: 789012
[INFO] Found root window: Kolobok - ... (HWND: 789012)
[INFO] Attempting capture using PrintWindow API...
[INFO] ✅ PrintWindow capture successful
```

## Что делать если проблема осталась?

### Проверка 1: Размер захваченного изображения

В Debug Viewer смотрите на:
```
Detection Info:
✅ Image: 1920x1080   ← Должен быть размер ПОЛНОГО окна, а не дочернего
```

Если размер маленький (например 1454x1044) - значит все еще захватывается дочернее окно.

### Проверка 2: Логи

Откройте вкладку **"Logs"** и найдите:
```
[WARNING] This is a child window! Parent HWND: XXX
```

Если видите это предупреждение - значит было выбрано дочернее окно.

### Решение: Выберите другое окно

В диалоге выбора окна могут быть несколько окон покера:
```
Kolobok - ... | python.exe | 1920x1080   ← Это главное окно! ✅
Kolobok - ... | python.exe | 1454x1044   ← Это дочернее окно ❌
```

**Выберите окно с БОЛЬШИМ размером** (обычно главное окно больше).

## Частые вопросы

**Q: Почему в списке несколько окон с одинаковым названием?**  
A: Приложение может создавать несколько окон. Главное окно обычно самое большое по размеру.

**Q: Диалог с предложением захватить root window не появляется**  
A: Возможно вы выбрали уже главное окно (не дочернее). Это хорошо! Продолжайте использовать.

**Q: Захват все равно показывает только стол без меню**  
A: Попробуйте:
1. Закройте Debug Viewer
2. Заново нажмите "Capture Window"
3. Выберите окно с МАКСИМАЛЬНЫМ размером (1920x1080 или больше)
4. Нажмите "Yes" в диалоге если появится
5. Откройте Debug Viewer снова

**Q: Можно ли захватывать дочернее окно специально?**  
A: Да! Нажмите "No" в диалоге если хотите захватить именно дочернее окно (например, только игровой стол без меню).

## Итог

### ✅ Что исправлено:
- Автоматическое определение дочерних окон
- Предложение захватить главное окно вместо дочернего
- Улучшенный метод захвата (PrintWindow)
- Детальное логирование процесса захвата

### 🎯 Результат:
Теперь захватывается **полное окно покера** со всем интерфейсом:
- ✅ Меню слева
- ✅ Игровой стол
- ✅ Все кнопки и элементы UI
- ✅ Правильный размер (1920x1080 вместо 1454x1044)

### 📝 Следующие шаги:
1. Заново захватите окно (Capture Window)
2. Выберите root window когда система предложит
3. Проверьте в Debug Viewer что захватывается полное окно
4. Настройте ROI зоны для полного окна

**Готово! Теперь бот видит полный интерфейс!** 🎉

---

⚠️ **EDUCATIONAL RESEARCH ONLY**

📖 См. также:
- `WINDOW_CAPTURE_FIXED.md` - общая инструкция по захвату окон
- `DEBUG_VIEWER_GUIDE.md` - руководство по Debug Viewer
- `START_HERE_DEBUG.md` - быстрый старт
