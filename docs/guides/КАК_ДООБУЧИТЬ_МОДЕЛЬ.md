# 🎓 КАК ДООБУЧИТЬ МОДЕЛЬ НА ОНЛАЙН-ПОКЕРЕ

## 📋 ЧТО НУЖНО:

- ✅ Python 3.8+
- ✅ Ultralytics YOLOv8
- ✅ 500-1000 скриншотов онлайн-покера
- ✅ 2-3 часа времени на разметку
- ✅ GPU (опционально, но быстрее)

---

## 🎯 ПОШАГОВАЯ ИНСТРУКЦИЯ

### ШАГ 1: Собрать скриншоты (30 минут)

#### Что нужно:
```
Минимум: 200 скриншотов
Оптимально: 500-1000 скриншотов
Идеально: 2000+ скриншотов
```

#### Как собирать:
```python
# Используйте встроенный инструмент захвата
# Откройте покер-рум и делайте скриншоты в разных ситуациях:

1. Разные стадии игры (префлоп, флоп, терн, ривер)
2. Разные позиции карт на столе
3. Разные освещения (если есть настройки темы)
4. Карты игроков, карты на борде
5. Закрытые и открытые карты
```

#### Автоматизация:
```python
# Создайте auto_screenshot.py
import time
from PIL import ImageGrab
import os

output_dir = "dataset/images"
os.makedirs(output_dir, exist_ok=True)

for i in range(500):
    time.sleep(10)  # Каждые 10 секунд
    screenshot = ImageGrab.grab()
    screenshot.save(f"{output_dir}/poker_{i:04d}.png")
    print(f"Скриншот {i+1}/500 сохранен")
```

---

### ШАГ 2: Разметить изображения (2-3 часа)

#### Вариант А: Roboflow (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Зарегистрируйтесь на https://roboflow.com
# 2. Создайте новый проект: "Online Poker Cards"
# 3. Загрузите свои скриншоты
# 4. Разметьте карты:
#    - Нарисуйте bounding box вокруг каждой карты
#    - Подпишите класс: AS, KH, QD, etc.
# 5. Экспортируйте в формате YOLOv8
```

**Преимущества Roboflow:**
- ✅ Веб-интерфейс, не нужно ничего устанавливать
- ✅ Автоматическая аугментация данных
- ✅ Готовый экспорт для YOLOv8
- ✅ Удобные инструменты разметки

#### Вариант Б: Label Studio (локально)

```bash
# Установка
pip install label-studio

# Запуск
label-studio start

# Откройте http://localhost:8080
# Создайте проект → Object Detection → YOLO
# Загрузите изображения
# Разметьте карты
```

#### Классы для разметки (52 карты):

```yaml
names:
  0: '10C'  # 10 треф
  1: '10D'  # 10 бубен
  2: '10H'  # 10 червей
  3: '10S'  # 10 пик
  4: '2C'   # 2 треф
  5: '2D'   # 2 бубен
  ... (всего 52 класса)
  50: 'QS'  # Дама пик
  51: 'KS'  # Король пик
```

---

### ШАГ 3: Подготовить датасет

#### Структура папок:
```
online_poker_dataset/
├── train/
│   ├── images/
│   │   ├── poker_0001.png
│   │   ├── poker_0002.png
│   │   └── ...
│   └── labels/
│       ├── poker_0001.txt
│       ├── poker_0002.txt
│       └── ...
├── valid/
│   ├── images/
│   └── labels/
├── test/
│   ├── images/
│   └── labels/
└── data.yaml
```

#### Создайте data.yaml:

```yaml
# online_poker_dataset/data.yaml

path: ./online_poker_dataset
train: train/images
val: valid/images
test: test/images

# Классы (все 52 карты)
names:
  0: '10C'
  1: '10D'
  2: '10H'
  3: '10S'
  4: '2C'
  5: '2D'
  6: '2H'
  7: '2S'
  8: '3C'
  9: '3D'
  # ... и так далее для всех 52 карт
```

---

### ШАГ 4: Дообучить модель

#### Простой способ (командная строка):

```bash
# Запустите обучение
yolo train \
  model=weights/best.pt \
  data=online_poker_dataset/data.yaml \
  epochs=100 \
  imgsz=640 \
  batch=16 \
  patience=20 \
  save=True \
  project=online_poker_training \
  name=yolov8_online_poker
```

#### С Python скриптом:

```python
# train_online_poker.py
from ultralytics import YOLO

# Загружаем предобученную модель
model = YOLO('weights/best.pt')

# Дообучаем на онлайн-покере
results = model.train(
    data='online_poker_dataset/data.yaml',
    epochs=100,
    imgsz=640,
    batch=16,
    patience=20,  # early stopping
    save=True,
    project='online_poker_training',
    name='yolov8_online_poker',
    
    # Augmentations для лучшей генерализации
    hsv_h=0.015,  # изменение оттенка
    hsv_s=0.7,    # изменение насыщенности
    hsv_v=0.4,    # изменение яркости
    degrees=5,    # поворот
    translate=0.1, # сдвиг
    scale=0.5,    # масштаб
    fliplr=0.5,   # горизонтальное отражение
)

print("Обучение завершено!")
print(f"Лучшая модель: {results.save_dir}/weights/best.pt")
```

#### Запустите:
```bash
python train_online_poker.py
```

---

### ШАГ 5: Протестировать модель

```python
# test_trained_model.py
from ultralytics import YOLO
from PIL import Image

# Загружаем обученную модель
model = YOLO('online_poker_training/yolov8_online_poker/weights/best.pt')

# Тестируем
test_image = "test_capture_overlay.png"
results = model(test_image, conf=0.25)

# Показываем результаты
result = results[0]
print(f"Найдено карт: {len(result.boxes)}")

for i, box in enumerate(result.boxes, 1):
    cls_id = int(box.cls[0])
    conf = float(box.conf[0])
    card = model.names[cls_id]
    print(f"{i}. {card} - {conf:.1%}")

# Сохраняем результат
result.save("test_trained_result.jpg")
print("Результат сохранен: test_trained_result.jpg")
```

---

### ШАГ 6: Использовать в проекте

```python
# Замените модель в yolo_detector.py
# Строка ~35:
local_models = [
    "online_poker_training/yolov8_online_poker/weights/best.pt",  # ВАША НОВАЯ МОДЕЛЬ!
    "weights/best.pt",  # Старая модель
    # ...
]
```

Или скопируйте новую модель:
```bash
copy "online_poker_training\yolov8_online_poker\weights\best.pt" "weights\online_poker_best.pt"
```

---

## ⚙️ ПАРАМЕТРЫ ОБУЧЕНИЯ

### Базовые (для начинающих):
```python
epochs=50          # Количество эпох
batch=16          # Размер батча (уменьшите до 8 если мало памяти)
imgsz=640         # Размер изображений
patience=10       # Остановка если нет улучшений
```

### Продвинутые (для опытных):
```python
epochs=100
batch=32          # Больше батч = быстрее, но нужно больше GPU памяти
imgsz=1280        # Больше размер = лучше детекция мелких карт
optimizer='AdamW' # Оптимизатор
lr0=0.001         # Начальная скорость обучения
warmup_epochs=3   # Прогрев модели
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После 50 эпох:
- **mAP50**: 80-90%
- **Точность**: 75-85%
- **Время**: 1-2 часа (с GPU)

### После 100 эпох:
- **mAP50**: 90-95%
- **Точность**: 85-95%
- **Время**: 2-4 часа (с GPU)

### С 1000+ изображений:
- **mAP50**: 95-99%
- **Точность**: 90-99%
- **Время**: 3-6 часов (с GPU)

---

## 💡 СОВЕТЫ ДЛЯ ЛУЧШИХ РЕЗУЛЬТАТОВ

### 1. Разнообразие данных:
```
✅ Разные стадии игры
✅ Разные позиции карт
✅ Разные темы интерфейса (если есть)
✅ Разные разрешения
✅ Карты частично закрыты
```

### 2. Качество разметки:
```
✅ Точные bounding boxes (плотно вокруг карты)
✅ Правильные классы (проверяйте AS vs AH!)
✅ Не пропускайте карты (размечайте ВСЕ видимые)
```

### 3. Аугментация:
```python
# Roboflow автоматически добавляет:
- Поворот ±5°
- Яркость ±20%
- Blur (легкое размытие)
- Flip (отражение)
```

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: Модель не обучается (loss не падает)
**Решение:**
```python
# Уменьшите learning rate
lr0=0.0001  # вместо 0.001
```

### Проблема: Модель переобучается (overfitting)
**Решение:**
```python
# Добавьте больше аугментаций
hsv_h=0.03
hsv_s=0.9
hsv_v=0.6
degrees=10
```

### Проблема: Мало памяти GPU
**Решение:**
```python
batch=4    # Уменьшите батч
imgsz=416  # Уменьшите размер
```

### Проблема: Модель путает карты (AS и AH)
**Решение:**
```
- Соберите больше данных с проблемными картами
- Проверьте качество разметки
- Увеличьте epochs до 150-200
```

---

## 📈 МОНИТОРИНГ ОБУЧЕНИЯ

### TensorBoard:
```bash
tensorboard --logdir online_poker_training/yolov8_online_poker
```

### Метрики:
```
- box_loss: Должна падать ↓
- cls_loss: Должна падать ↓
- dfl_loss: Должна падать ↓
- mAP50: Должна расти ↑
- mAP50-95: Должна расти ↑
```

---

## 🎉 ГОТОВО!

После дообучения у вас будет модель, которая:

✅ Распознает карты на ВАШЕМ конкретном руме  
✅ Точность 85-99% (зависит от данных)  
✅ Работает с 3D-картами онлайн-покера  
✅ Настроена под ваш интерфейс  

**Время:** 3-4 часа работы = идеальная модель!

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- Ultralytics Docs: https://docs.ultralytics.com
- Roboflow Tutorial: https://blog.roboflow.com/train-yolov8/
- Label Studio Guide: https://labelstud.io/guide/
- YOLOv8 Fine-tuning: https://docs.ultralytics.com/modes/train/

**Вопросы? Спрашивайте!**
