# 🎴 Руководство по тестированию OCR

## 📌 Важно понять разницу

### ❌ HTML интерфейс (poker_overlay_advanced.html)
- **Назначение**: Настройка зон ROI (координаты областей)
- **Кнопка "Тест распознавания"**: Показывает СЛУЧАЙНЫЕ карты (симуляция для проверки UI)
- **НЕ распознает** реальные карты!

### ✅ Реальное OCR распознавание
- **Использует**: Python + Tesseract OCR
- **Анализирует**: Реальные скриншоты
- **Использует**: Настроенные вами зоны из `poker_table_config.yaml`

---

## 🚀 Шаги для настройки реального OCR

### Шаг 1: Подготовка файлов

1. **Скачанный конфиг**
   ```
   Скопируйте: poker_table_config (1).yaml
   В папку проекта: c:\proekt-i\Tg_Pkr_Bot\
   Переименуйте в: poker_table_config.yaml
   ```

2. **Скриншот покерного стола**
   ```
   Сохраните скриншот того же стола, где настраивали зоны
   Назовите: screenshot_test.png
   Положите в: c:\proekt-i\Tg_Pkr_Bot\
   ```

### Шаг 2: Установка Tesseract OCR

#### Windows:

1. Скачайте установщик:
   - https://github.com/UB-Mannheim/tesseract/wiki
   - Или: https://digi.bib.uni-mannheim.de/tesseract/tesseract-ocr-w64-setup-5.3.3.20231005.exe

2. Установите в стандартную папку:
   ```
   C:\Program Files\Tesseract-OCR
   ```

3. Добавьте в PATH (PowerShell от имени администратора):
   ```powershell
   $env:Path += ";C:\Program Files\Tesseract-OCR"
   [Environment]::SetEnvironmentVariable("Path", $env:Path, [System.EnvironmentVariableTarget]::Machine)
   ```

4. Проверьте установку:
   ```bash
   tesseract --version
   ```

### Шаг 3: Установка Python зависимостей

```bash
# Активируйте виртуальное окружение
.\.venv\Scripts\Activate.ps1

# Установите зависимости для OCR
pip install pillow pytesseract pyyaml
```

### Шаг 4: Запуск теста OCR

```bash
# Базовый запуск (использует файлы по умолчанию)
python test_real_ocr.py

# Или укажите пути к файлам явно
python test_real_ocr.py screenshot_test.png poker_table_config.yaml

# Или используйте точные имена из загрузок
python test_real_ocr.py "photo_2026-01-16_19-35-24.jpg" "poker_table_config (1).yaml"
```

---

## 📊 Что делает тест OCR

1. ✅ Загружает конфигурацию зон из YAML
2. ✅ Загружает ваш скриншот
3. ✅ Создает визуализацию зон (`zones_visualization.png`)
4. ✅ Распознает текст в каждой зоне:
   - 🃏 Карты героя (hero_card_1, hero_card_2)
   - 🎴 Борд (board_card_1-5)
   - 💰 Банк (pot)
   - 💵 Стек героя (hero_stack)
   - 👥 Оппоненты (opponent_1-5)
5. ✅ Выводит результаты в консоль

---

## 🔍 Проверка результатов

### 1. Визуализация зон
После запуска откройте файл:
```
zones_visualization.png
```

**Проверьте:**
- ✅ Зоны точно охватывают карты?
- ✅ Зоны не выходят за границы карт?
- ✅ Зоны не перекрываются?

### 2. Распознанный текст
В консоли вы увидите что-то вроде:
```
🃏 КАРТЫ ГЕРОЯ
hero_card_1: 'As'
hero_card_2: 'Kh'

🎴 БОРД (ОБЩИЕ КАРТЫ)
ФЛОП #1 (board_card_1): 'Qd'
ФЛОП #2 (board_card_2): '8c'
ФЛОП #3 (board_card_3): 'Qs'
```

---

## 🛠️ Если распознавание неточное

### Проблема 1: Зоны неправильно расположены
**Решение:**
1. Откройте `zones_visualization.png`
2. Если зоны сдвинуты - вернитесь в HTML интерфейс
3. Перенастройте зоны ROI
4. Скачайте новый конфиг
5. Повторите тест

### Проблема 2: Tesseract не распознает карты
**Решение:**
1. Убедитесь, что на скриншоте карты четкие и крупные
2. Попробуйте настроить Tesseract параметры в коде:
   ```python
   # В test_real_ocr.py, функция extract_text_from_roi
   text = pytesseract.image_to_string(cropped, 
       config='--psm 10 --oem 3 -c tessedit_char_whitelist=AKQJT98765432shdc')
   ```

3. Используйте скриншот с высоким разрешением

### Проблема 3: Карты распознаются, но неправильно
**Причины:**
- Низкое разрешение
- Карты слишком стилизованные (нестандартные шрифты)
- Плохое освещение/контраст

**Решение:**
- Используйте классический вид карт в клиенте
- Увеличьте размер стола в настройках покер-клиента
- Делайте скриншот в полноэкранном режиме

---

## 🎯 Следующие шаги после успешного теста

### 1. Интеграция с API

Запустите Python API:
```bash
uvicorn coach_app.api.main:app --reload --port 8000
```

Отправьте скриншот для анализа:
```bash
curl -X POST "http://127.0.0.1:8000/analyze/poker/screenshot" ^
  -F "image=@screenshot_test.png"
```

### 2. Live RTA (Real-Time Assistance)

Запустите live-режим с вашим конфигом:
```bash
python -m coach_app.rta.live_rta --config poker_table_config.yaml --mode overlay
```

**Режимы:**
- `console` - вывод в консоль
- `overlay` - прозрачный оверлей поверх стола
- `telegram` - отправка в Telegram

### 3. TypeScript/React версия (более продвинутая)

Если хотите полный стек с GTO:
```bash
# Backend
cd backend
npm install
npm run dev

# Frontend
cd frontend
npm install
npm run dev
```

---

## 📚 Дополнительные ресурсы

- **Документация проекта**: `/docs/LIVE_RTA.md`
- **API документация**: `/docs/API.md`
- **Настройка адаптеров**: `/docs/SETUP.md`
- **Конфиги примеров**: `coach_app/configs/vision_adapters/`

---

## ❓ Часто задаваемые вопросы

### В: Почему HTML показывает неправильные карты?
**О:** HTML интерфейс - это только для настройки зон. Кнопка "Тест распознавания" там - просто демо с рандомными картами.

### В: Как улучшить точность распознавания?
**О:** 
1. Используйте высокое разрешение (1920x1080+)
2. Настройте зоны максимально точно
3. Используйте классический вид карт
4. Настройте Tesseract whitelist для карточных символов

### В: Можно ли использовать для live игры?
**О:** Да, но соблюдайте правила площадки! По умолчанию используется режим `--ethical` (подсказки только после действия).

### В: Работает ли с любым покер-клиентом?
**О:** Да, если вы настроите зоны ROI под конкретный клиент в HTML интерфейсе.

---

**Удачи с настройкой! 🎴🚀**
