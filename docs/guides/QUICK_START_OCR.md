# ⚡ Быстрый старт: Тест OCR

## 🎯 Что нужно сделать

### 1️⃣ Подготовьте файлы

```
📁 c:\proekt-i\Tg_Pkr_Bot\
   ├── poker_table_config (1).yaml  ← Скачанный из браузера
   ├── photo_2026-01-16_19-35-24.jpg  ← Скриншот покерного стола
   └── quick_ocr_test.bat  ← Скрипт для запуска
```

**Где взять:**
- ✅ Конфиг: Уже скачали из HTML интерфейса
- ✅ Скриншот: Тот же самый, что использовали для настройки зон

### 2️⃣ Установите Tesseract (если еще не установлен)

**Windows:**
```
1. Скачайте: https://github.com/UB-Mannheim/tesseract/wiki
2. Установите в: C:\Program Files\Tesseract-OCR
3. Добавьте в PATH (или перезапустите компьютер)
```

**Проверка:**
```bash
tesseract --version
```

### 3️⃣ Запустите тест

**Вариант 1: Автоматический скрипт (рекомендуется)**
```bash
quick_ocr_test.bat
```

**Вариант 2: Вручную**
```bash
# Активируйте окружение
.\.venv\Scripts\Activate.ps1

# Установите зависимости
pip install pillow pytesseract pyyaml

# Запустите тест
python test_real_ocr.py "photo_2026-01-16_19-35-24.jpg" "poker_table_config (1).yaml"
```

### 4️⃣ Проверьте результаты

1. **В консоли** увидите распознанные карты:
   ```
   🃏 КАРТЫ ГЕРОЯ
   hero_card_1: 'As'
   hero_card_2: 'Kh'
   
   🎴 БОРД
   ФЛОП #1: 'Qd'
   ФЛОП #2: '8c'
   ...
   ```

2. **Файл `zones_visualization.png`** - посмотрите, правильно ли расположены зоны

---

## ❌ Проблемы и решения

### "Tesseract not found"
```bash
# Установите Tesseract и добавьте в PATH
# Или укажите путь в коде:
# В test_real_ocr.py добавьте в начало:
pytesseract.pytesseract.tesseract_cmd = r'C:\Program Files\Tesseract-OCR\tesseract.exe'
```

### "Карты распознаются неправильно"
1. Откройте `zones_visualization.png`
2. Если зоны сдвинуты → вернитесь в `poker_overlay_advanced.html`
3. Перенастройте зоны точнее
4. Скачайте новый конфиг
5. Повторите тест

### "Зоны правильные, но текст неточный"
- Используйте скриншот с высоким разрешением
- Убедитесь, что карты четкие и крупные
- Попробуйте классический вид карт в покер-клиенте

---

## 📊 Что происходит под капотом

```
HTML интерфейс (poker_overlay_advanced.html)
  │
  ├─ Настройка зон ROI (x, y, w, h)
  │   Кнопка "Тест распознавания" = ДЕМО (случайные карты)
  │
  └─ Экспорт → poker_table_config.yaml
                      │
                      ▼
              test_real_ocr.py
                      │
                      ├─ Загружает конфиг зон
                      ├─ Загружает скриншот
                      ├─ Вырезает каждую зону
                      ├─ Распознает через Tesseract
                      └─ Выводит результаты
```

---

## ✅ После успешного теста

### Используйте API для анализа:
```bash
# Запустите API
uvicorn coach_app.api.main:app --reload --port 8000

# Отправьте скриншот
curl -X POST "http://127.0.0.1:8000/analyze/poker/screenshot" ^
  -F "image=@screenshot_test.png"
```

### Или Live RTA:
```bash
python -m coach_app.rta.live_rta ^
  --config poker_table_config.yaml ^
  --mode overlay
```

---

**Полная инструкция**: `OCR_TESTING_GUIDE.md`
